# MNIST-digit-prediction
The MNIST Digit Prediction project is a machine learning and deep learning application that classifies handwritten digits (0–9) using the MNIST dataset. The dataset consists of thousands of grayscale images of handwritten digits, each labeled with its corresponding numerical value. 
